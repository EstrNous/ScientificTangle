---
description: ScientificTangle — hard rules (L0)
alwaysApply: true
---

Следуй `AGENTS.md` и `docs/agent_context/task_router.md`. Эталон: `docs/agent_prompts/every_chat.md`.

Hard rules: ветки `feat/*` (dev по запросу); не трогать `main`; не локальный merge в `dev` — только PR; `git fetch` + rebase на feat; коммиты `feat: ...` на русском; без комментариев в коде; relative imports в `services/*/app/`; confirmed claims с `SourceSpan`; `README.md` не менять без запроса.
